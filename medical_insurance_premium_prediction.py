# -*- coding: utf-8 -*-
"""Medical_Insurance_Premium_Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1G5xU4KiwZxfq4aLIqyeemvRmHi5hy7eE

# Data Collection
Data collection adalah proses mengumpulkan informasi atau data yang diperlukan untuk analisis, penelitian, atau pengambilan keputusan. Proses ini dapat dilakukan melalui berbagai metode, seperti survei, wawancara, observasi, atau penggunaan data yang sudah ada.

Tujuan dari pengumpulan data adalah untuk mendapatkan wawasan yang lebih baik tentang suatu fenomena, membantu dalam penelitian ilmiah, atau mendukung pengembangan produk dan layanan. Data yang dikumpulkan bisa berupa kuantitatif (angka) atau kualitatif (deskripsi).
"""

# Commented out IPython magic to ensure Python compatibility.
import os
import requests
import zipfile
import io
import numpy as np
import matplotlib.pyplot as plt
import pandas as pd
import seaborn as sns
import warnings
warnings.filterwarnings('ignore')

# %matplotlib inline

!gdown --id "1wGD2leYR24xpxYbihVfyjFSo9Ir_iFJu"

pd.set_option('display.max_columns', None)
pd.set_option('display.max_rows', None)
pd.set_option('display.float_format', lambda x: '%.3f' % x)

df = pd.read_csv("/content/Medicalpremium.csv")
df.head(10)

"""# Data Understanding & Removing Outlier
Data understanding dan penghapusan outlier adalah langkah penting dalam analisis data. Berikut penjelasan tentang masing-masing:

Data Understanding
Data understanding adalah proses eksplorasi dan pemahaman tentang data yang telah dikumpulkan. Langkah-langkah yang umumnya dilakukan meliputi:

Deskripsi Data: Mengetahui karakteristik dasar dari dataset, seperti ukuran, jenis variabel, dan struktur data.
Statistik Deskriptif: Menghitung nilai-nilai statistik dasar seperti mean, median, modus, rentang, dan deviasi standar untuk memahami distribusi data.
Visualisasi Data: Menggunakan grafik seperti histogram, box plot, atau scatter plot untuk melihat pola, tren, dan hubungan antar variabel.
Identifikasi Masalah: Mencari missing values, kesalahan input, dan anomali dalam data.
Menghapus Outlier
Outlier adalah nilai ekstrem yang berbeda jauh dari nilai lain dalam dataset. Mereka dapat memengaruhi hasil analisis dan model secara signifikan. Berikut adalah beberapa metode untuk mengidentifikasi dan menghapus outlier:

Metode Statistika:

Z-score: Menghitung z-score untuk setiap data; nilai z-score lebih dari 3 (atau kurang dari -3) dapat dianggap sebagai outlier.
IQR (Interquartile Range): Menghitung Q1 (kuartil pertama) dan Q3 (kuartil ketiga); outlier dapat ditentukan sebagai nilai yang berada di luar rentang
𝑄
1
−
1.5
×
𝐼
𝑄
𝑅
Q1−1.5×IQR dan
𝑄
3
+
1.5
×
𝐼
𝑄
𝑅
Q3+1.5×IQR.
Visualisasi:

Box Plot: Menunjukkan distribusi data dan dengan jelas mengidentifikasi outlier.
Scatter Plot: Menampilkan hubungan antar variabel dan membantu dalam melihat outlier.
Domain Knowledge: Mempertimbangkan konteks atau domain spesifik untuk menentukan apakah outlier tersebut valid atau harus dihapus.

Penanganan Outlier
Setelah mengidentifikasi outlier, Anda dapat memilih untuk:

Menghapus outlier dari dataset.
Mengganti outlier dengan nilai yang lebih representatif (misalnya, mean atau median).
Menggunakan model yang lebih robust terhadap outlier.
Penghapusan outlier harus dilakukan dengan hati-hati, karena kadang-kadang outlier bisa jadi informasi yang berharga.
"""

df.info()

df.columns

len(df)

df.shape

df.describe()

"""# Handling Missing Value
Handling missing values adalah langkah penting dalam analisis data, karena nilai yang hilang dapat memengaruhi hasil analisis dan model. Berikut adalah beberapa metode umum untuk menangani missing values:

1. Identifikasi Missing Values
Sebelum menangani missing values, penting untuk mengidentifikasinya. Ini bisa dilakukan dengan:

Menggunakan Fungsi: Dalam banyak bahasa pemrograman, ada fungsi untuk menghitung jumlah missing values dalam setiap kolom.
Visualisasi: Menggunakan heatmap atau grafik lainnya untuk melihat pola missing values.
2. Metode Penanganan Missing Values
a. Menghapus Data
Penghapusan Baris: Jika hanya ada sedikit baris yang memiliki nilai hilang, Anda dapat menghapus baris tersebut.
Penghapusan Kolom: Jika sebuah kolom memiliki persentase missing values yang sangat tinggi, Anda dapat mempertimbangkan untuk menghapus kolom tersebut.
b. Mengisi Missing Values
Mean/Median/Mode Imputation: Mengisi nilai yang hilang dengan rata-rata (mean), median, atau modus dari kolom tersebut.
Forward/Backward Fill: Mengisi nilai yang hilang dengan nilai sebelumnya (forward fill) atau nilai berikutnya (backward fill), biasanya digunakan dalam data waktu.
Interpolate: Menggunakan interpolasi untuk mengisi nilai yang hilang berdasarkan nilai di sekitarnya.
c. Model-Based Imputation
Predictive Modeling: Menggunakan model regresi atau algoritma pembelajaran mesin untuk memprediksi nilai yang hilang berdasarkan variabel lain dalam dataset.
K-Nearest Neighbors (KNN): Menggunakan nilai dari K tetangga terdekat untuk mengisi nilai yang hilang.
3. Evaluasi
Setelah menangani missing values, penting untuk mengevaluasi dampak metode yang digunakan terhadap dataset dan analisis. Perhatikan apakah ada perubahan signifikan dalam distribusi data atau hasil model.

4. Catatan
Pilihan metode harus didasarkan pada sifat dan konteks data. Misalnya, menggunakan mean untuk data yang memiliki distribusi normal, sedangkan median lebih cocok untuk data yang memiliki outlier.
Pastikan untuk mencatat metode yang digunakan untuk menangani missing values agar dapat direplikasi atau dianalisis lebih lanjut di masa mendatang.
Menangani missing values dengan tepat dapat membantu meningkatkan kualitas dan akurasi analisis data.

"""

df.isnull().sum()

df.isnull().any()

df.isna().sum()

df.isna().any()

"""# Handling Outliers
Statistik Deskriptif: Menggunakan z-score atau IQR (Interquartile Range).

Z-score: Menghitung z-score untuk setiap nilai; nilai di luar batas tertentu (misalnya, lebih dari 3 atau kurang dari -3) dianggap outlier.
IQR: Menghitung Q1 (kuartil pertama) dan Q3 (kuartil ketiga); outlier adalah nilai di luar rentang
𝑄
1
−
1.5
×
𝐼
𝑄
𝑅
Q1−1.5×IQR dan
𝑄
3
+
1.5
×
𝐼
𝑄
𝑅
Q3+1.5×IQR.
"""

sns.boxplot(x=df['Age'], color='red')
plt.show()

sns.boxplot(x=df['Height'], color='green')

sns.boxplot(x=df['Weight'], color='purple')

sns.boxplot(x=df['NumberOfMajorSurgeries'], color='skyblue')

Q1 = df.quantile(0.25)
Q3 = df.quantile(0.75)
IQR=Q3-Q1
insurance=df[~((df<(Q1-1.5*IQR))|(df>(Q3+1.5*IQR))).any(axis=1)]

# Cek ukuran dataset setelah kita drop outliers
insurance.shape

"""# Univariate Analysis
Univariate analysis adalah analisis yang fokus pada satu variabel tunggal dalam suatu dataset. Tujuannya adalah untuk memahami karakteristik, distribusi, dan pola dari variabel tersebut tanpa mempertimbangkan hubungan dengan variabel lain. Berikut adalah beberapa aspek penting dalam univariate analysis:

1. Jenis Variabel
Univariate analysis dapat dilakukan pada dua jenis variabel:

Variabel Kualitatif (Kategorikal): Misalnya, jenis kelamin, status perkawinan, atau warna favorit.
Variabel Kuantitatif (Numerik): Misalnya, umur, tinggi badan, atau penghasilan.
2. Statistik Deskriptif
Statistik deskriptif digunakan untuk memberikan gambaran umum tentang variabel:

Untuk Variabel Kualitatif:

Frekuensi dan proporsi untuk setiap kategori.
Grafik seperti pie chart atau bar chart untuk visualisasi distribusi kategori.
Untuk Variabel Kuantitatif:

Ukuran pusat: mean, median, dan modus.
Ukuran dispersi: rentang, varians, dan deviasi standar.
Grafik seperti histogram, box plot, atau density plot untuk melihat distribusi nilai.
3. Visualisasi
Visualisasi sangat penting dalam univariate analysis karena membantu dalam memahami pola data:

Histogram: Menampilkan distribusi frekuensi dari variabel kuantitatif.
Box Plot: Menunjukkan median, kuartil, dan outliers dalam data kuantitatif.
Bar Chart: Digunakan untuk menampilkan frekuensi kategori dalam variabel kualitatif.
4. Analisis Distribusi
Normalitas: Memeriksa apakah data mengikuti distribusi normal menggunakan uji seperti Shapiro-Wilk atau visualisasi seperti Q-Q plot.
Skewness dan Kurtosis: Mengukur simetri dan puncak distribusi data.
5. Kesimpulan
Univariate analysis memberikan wawasan penting tentang satu variabel yang dapat membantu dalam pengambilan keputusan, pemilihan model, dan pemahaman data secara keseluruhan. Dengan melakukan analisis ini, Anda dapat mengidentifikasi pola, tren, dan anomali yang mungkin ada dalam data sebelum melanjutkan ke analisis multivariate.
"""

cat_features = ['Diabetes', 'BloodPressureProblems', 'AnyTransplants', 'AnyChronicDiseases', 'KnownAllergies', 'HistoryOfCancerInFamily']
num_features = ['Age', 'Height', 'Weight', 'NumberOfMajorSurgeries', 'PremiumPrice']

"""**Categorical Features**"""

import matplotlib.pyplot as plt
plt.subplots(2, 3, figsize=(20, 16))
colors = ['red', 'green', 'blue', 'orange', 'purple', 'skyblue']
for i, col in enumerate(cat_features):
  plt.subplot(2, 3, i + 1)
  df.groupby(col).size().plot(kind='bar', rot=0, color=colors[i])
  plt.title(col)
plt.tight_layout()
plt.show()

feature = cat_features[0]
count = df[feature].value_counts()
percent = 100*df[feature].value_counts(normalize=True)
df0 = pd.DataFrame({'jumlah sampel':count, 'persentase':percent.round(1)})
print(df0)
count.plot(kind='bar', title=feature);

feature = cat_features[1]
count = df[feature].value_counts()
percent = 100*df[feature].value_counts(normalize=True)
df1 = pd.DataFrame({'jumlah sampel':count, 'persentase':percent.round(1)})
print(df1)
count.plot(kind='bar', title=feature);

feature = cat_features[2]
count = df[feature].value_counts()
percent = 100*df[feature].value_counts(normalize=True)
df2 = pd.DataFrame({'jumlah sampel':count, 'persentase':percent.round(1)})
print(df2)
count.plot(kind='bar', title=feature);

feature = cat_features[3]
count = df[feature].value_counts()
percent = 100*df[feature].value_counts(normalize=True)
df3 = pd.DataFrame({'jumlah sampel':count, 'persentase':percent.round(1)})
print(df3)
count.plot(kind='bar', title=feature);

feature = cat_features[4]
count = df[feature].value_counts()
percent = 100*df[feature].value_counts(normalize=True)
df4 = pd.DataFrame({'jumlah sampel':count, 'persentase':percent.round(1)})
print(df4)
count.plot(kind='bar', title=feature);

feature = cat_features[5]
count = df[feature].value_counts()
percent = 100*df[feature].value_counts(normalize=True)
df5 = pd.DataFrame({'jumlah sampel':count, 'persentase':percent.round(1)})
print(df5)
count.plot(kind='bar', title=feature);

"""**Numerical Features**"""

import matplotlib.pyplot as plt
df.hist(bins=50, figsize=(20,15), color='green')
plt.show()

"""# Multivariate Analysis
Multivariate analysis adalah teknik statistik yang digunakan untuk menganalisis lebih dari satu variabel secara bersamaan. Tujuannya adalah untuk memahami hubungan antara variabel-variabel tersebut dan bagaimana mereka berinteraksi satu sama lain. Berikut adalah beberapa konsep penting dalam multivariate analysis:

1. Tujuan Multivariate Analysis
Identifikasi Hubungan: Mengetahui apakah ada hubungan antara variabel-variabel dan seberapa kuat hubungan tersebut.
Pengelompokan Data: Mengelompokkan observasi yang memiliki kesamaan.
Prediksi: Menggunakan variabel independen untuk memprediksi variabel dependen.
2. Jenis Analisis Multivariate
Beberapa teknik analisis multivariate yang umum digunakan antara lain:

a. Regresi Multivariat
Memungkinkan untuk memprediksi nilai satu variabel dependen berdasarkan beberapa variabel independen.
Contohnya adalah regresi linier berganda, di mana satu variabel dependen diprediksi menggunakan lebih dari satu variabel independen.
b. Analisis Korespondensi
Digunakan untuk menganalisis hubungan antara dua variabel kategorikal, membantu dalam memahami pola dalam tabel kontingensi.
c. Analisis Faktor
Mengidentifikasi struktur yang mendasari data dengan mengelompokkan variabel yang berkorelasi tinggi.
Digunakan untuk mereduksi dimensi data dan menemukan faktor latar belakang.
d. Analisis Cluster
Mengelompokkan data ke dalam kelompok (cluster) yang memiliki kemiripan, berdasarkan sejumlah variabel.
Contohnya, K-means clustering.
e. MANOVA (Multivariate Analysis of Variance)
Digunakan untuk menguji perbedaan antara kelompok pada lebih dari satu variabel dependen sekaligus.
3. Visualisasi
Visualisasi sangat penting dalam multivariate analysis untuk membantu memahami hubungan antar variabel:

Scatter Plot Matrices: Menampilkan hubungan antara setiap pasangan variabel.
Heatmaps: Menggambarkan kekuatan hubungan antara variabel dalam bentuk warna.
Parallel Coordinates Plot: Menunjukkan multivariat data dalam format grafis yang memungkinkan untuk melihat pola.
4. Evaluasi Model
Setelah melakukan analisis, penting untuk mengevaluasi model dan hasilnya, seperti:

Koefisien Determinasi (R²): Mengukur seberapa baik model menjelaskan variasi dalam data.
Uji Hipotesis: Menggunakan uji statistik untuk mengevaluasi signifikansi hubungan.
5. Catatan
Pemilihan teknik analisis multivariate harus didasarkan pada sifat data dan tujuan analisis.
Hasil dari multivariate analysis dapat memberikan wawasan yang mendalam, tetapi juga harus diinterpretasikan dengan hati-hati untuk menghindari kesimpulan yang salah.
Multivariate analysis adalah alat yang kuat untuk memahami kompleksitas data dan membuat keputusan yang lebih terinformasi.

**Categorical Features**
"""

cat_features = df.select_dtypes(include='object').columns.to_list()

for col in cat_features:
  sns.catplot(x=col, y="PremiumPrice", kind="bar", color='red', dodge=False, height = 4, aspect = 3,  data=df, palette="Set3")
  plt.title("Rata-rata 'PremiumPrice' Relatif terhadap - {}".format(col))

"""**Numerical Features**"""

sns.pairplot(df, diag_kind = 'kde', corner = True)
plt.show()

plt.figure(figsize=(10, 8))
correlation_matrix = df.corr().round(2)

# Untuk print nilai di dalam kotak, gunakan parameter anot=True
sns.heatmap(data=correlation_matrix, annot=True, cmap='PiYG' )
plt.title("Correlation Matrix untuk Fitur Numerik ", size=20)

"""# Data Preparation
Data preparation adalah langkah penting dalam proses analisis data yang melibatkan pembersihan, transformasi, dan pengorganisasian data sebelum digunakan untuk analisis lebih lanjut atau pengembangan model.
"""

#One Hot Encoding
data_cat =  pd.get_dummies(df[['Diabetes', 'BloodPressureProblems', 'AnyTransplants', 'AnyChronicDiseases', 'KnownAllergies', 'HistoryOfCancerInFamily']])

data_cat

# Concenate Dataframe
df = pd.concat([df,data_cat],axis=1)

df.drop(columns=['Diabetes', 'BloodPressureProblems', 'AnyTransplants', 'AnyChronicDiseases', 'KnownAllergies', 'HistoryOfCancerInFamily'],inplace=True)

# Split Dataset
from sklearn.model_selection import train_test_split

X = df.drop(['PremiumPrice'],axis =1)
y = df['PremiumPrice']
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size = 0.2, random_state = 123)

print(f'Total # of sample in whole dataset: {len(X)}')
print(f'Total # of sample in train dataset: {len(X_train)}')
print(f'Total # of sample in test dataset: {len(X_test)}')

print(f'Total # of sample in whole dataset: {len(y)}')
print(f'Total # of sample in train dataset: {len(y_train)}')
print(f'Total # of sample in test dataset: {len(y_test)}')

"""# Model Development
Model development adalah tahap dalam proses analisis data di mana Anda membangun, melatih, dan mengevaluasi model untuk melakukan prediksi atau analisis berdasarkan data yang telah dipersiapkan

Dalam konteks asuransi kesehatan (medical insurance), baik Huber Regression maupun Support Vector Regression (SVR) dapat digunakan untuk memprediksi biaya klaim medis berdasarkan berbagai fitur, seperti usia, jenis kelamin, riwayat kesehatan, dan faktor lainnya. Mari kita bahas keduanya lebih mendalam:

1. Huber Regression
Penjelasan
Huber Regression adalah metode regresi yang robust terhadap outliers. Dalam konteks asuransi kesehatan, data klaim sering kali mengandung outliers, seperti klaim yang sangat tinggi akibat kondisi kesehatan serius atau kesalahan pengisian data. Huber Regression membantu mengatasi pengaruh outlier ini dengan cara yang lebih baik dibandingkan regresi linier biasa.

Keunggulan
Ketahanan terhadap Outliers: Karena klaim kesehatan dapat sangat bervariasi, Huber Regression akan memberikan hasil yang lebih stabil dan andal, tanpa terlalu terpengaruh oleh nilai ekstrem.
Fleksibilitas: Model ini dapat digunakan untuk memperkirakan biaya klaim, yang membantu perusahaan asuransi dalam mengelola risiko dan merumuskan premi.
Penggunaan
Misalnya, jika perusahaan asuransi ingin memprediksi biaya klaim untuk polis tertentu, Huber Regression dapat digunakan untuk menghasilkan estimasi biaya yang lebih realistis dengan mempertimbangkan semua data, termasuk outliers.

2. Support Vector Regression (SVR)
Penjelasan
Support Vector Regression (SVR) adalah metode yang menggunakan prinsip Support Vector Machines untuk melakukan regresi. SVR berfungsi dengan menemukan hyperplane yang paling optimal dalam ruang multidimensi untuk memisahkan data, dengan tujuan meminimalkan kesalahan prediksi.

Keunggulan
Fleksibilitas Model: SVR dapat menangani data dengan kompleksitas tinggi dan interaksi nonlinear antara fitur, yang sering terjadi dalam data asuransi kesehatan.
Kontrol atas Kesalahan: Parameter epsilon dalam SVR memungkinkan penentuan toleransi kesalahan, sehingga perusahaan asuransi dapat mengontrol seberapa besar kesalahan yang dapat diterima dalam estimasi biaya.
Penggunaan
Contohnya, SVR dapat digunakan untuk memprediksi biaya perawatan untuk pasien berdasarkan faktor-faktor seperti usia, jenis kelamin, riwayat penyakit, dan faktor gaya hidup. Ini membantu perusahaan asuransi dalam menentukan premi yang lebih adil dan akurat untuk pemegang polis.

Perbandingan dalam Konteks Asuransi Kesehatan
Robustness: Huber Regression lebih unggul dalam mengatasi outliers, yang sering muncul dalam data klaim medis.
Kekuatan Nonlinearitas: SVR lebih baik dalam menangkap hubungan kompleks dan nonlinear antara fitur, yang mungkin ada dalam data asuransi kesehatan.
Kesimpulan
Kedua model ini dapat memberikan wawasan berharga bagi perusahaan asuransi kesehatan dalam memprediksi biaya klaim. Pilihan antara Huber Regression dan SVR akan bergantung pada karakteristik spesifik dari dataset dan kebutuhan analisis. Misalnya, jika data mengandung banyak outliers, Huber Regression mungkin menjadi pilihan yang lebih baik, sedangkan jika ada interaksi kompleks yang perlu ditangkap, SVR bisa lebih efektif.
"""

# Dataframe untuk menyimpan hasil
result = pd.DataFrame(index=['train_mse', 'test_mse','eval_train','eval_test'],
                      columns=['Huber', 'SVR'])

from sklearn.linear_model import HuberRegressor
from sklearn.svm import SVR
from sklearn.metrics import mean_squared_error

# Train Model Huber
hr = HuberRegressor()
hr.fit(X_train, y_train)

result.loc['train_mse','Huber'] = mean_squared_error(y_pred = hr.predict(X_train), y_true=y_train)

# Train Model SVR
svr = SVR()
svr.fit(X_train, y_train)

result.loc['train_mse','SVR'] = mean_squared_error(y_pred = svr.predict(X_train), y_true=y_train)

# Prediksi data testing dan simpan hasil ke dataframe
result.loc['test_mse','Huber'] = mean_squared_error(y_pred = hr.predict(X_test), y_true=y_test)
result.loc['test_mse','SVR'] = mean_squared_error(y_pred = svr.predict(X_test), y_true=y_test)

result

"""# Model Evaluation
Model evaluation adalah tahap penting dalam proses pengembangan model yang bertujuan untuk menilai seberapa baik model yang telah dibangun dalam memprediksi atau mengklasifikasikan data.
"""

result.plot(kind='bar')

from sklearn.model_selection import GridSearchCV

# Hyperparams Menggunakan Grid Search pada Huber
hr_eval = HuberRegressor()
param_grid = { #Setup Params
    'epsilon': [1.0, 1.5, 2.0],
    'alpha': [0.0001, 0.001, 0.01],
    'max_iter': [100, 200, 300]
}

grid_search_huber = GridSearchCV(hr_eval, param_grid, scoring='neg_mean_squared_error', cv=5)

# Train Model dengan hyperparam
grid_search_huber.fit(X_train, y_train)

print("Best hyperparameters:", grid_search_huber.best_params_)
print("Best Score:", grid_search_huber.best_score_)

# Data Testing
result.loc['eval_train','Huber'] = mean_squared_error(y_pred = grid_search_huber.predict(X_train), y_true=y_train)
result.loc['eval_test','Huber'] = mean_squared_error(y_pred = grid_search_huber.predict(X_test), y_true=y_test)

# Hyperparams Menggunakan Grid Search pada SVR
svr_eval = SVR()
param_grid_svr = { #Setup Params
    'kernel': ['linear', 'rbf'],
    'C': [0.1, 1, 10],
    'epsilon': [0.1, 0.2, 0.3]
}
# Train models menggunakan hyperparams
grid_search_svr = GridSearchCV(svr_eval, param_grid_svr, scoring='neg_mean_squared_error', cv=5)
grid_search_svr.fit(X_train, y_train)

print("Best hyperparameters:", grid_search_svr.best_params_)
print("Best Score:", grid_search_svr.best_score_)

# Data Testing
result.loc['eval_train','SVR'] = mean_squared_error(y_pred = grid_search_svr.predict(X_train), y_true=y_train)
result.loc['eval_test','SVR'] = mean_squared_error(y_pred = grid_search_svr.predict(X_test), y_true=y_test)

result

result.plot(kind='bar')

"""# Kesimpulan

1. Metrik Kinerja
Train MSE (Mean Squared Error)
Huber: 18,480,694.57
SVR: 40,114,366.68
Test MSE
Huber: 25,177,990.88
SVR: 42,742,505.68
Eval Train
Huber: 18,104,561.09
SVR: 18,614,533.70
Eval Test
Huber: 24,212,812.87
SVR: 25,605,704.54
2. Analisis Hasil
Train MSE
Huber Regression memiliki MSE yang jauh lebih rendah dibandingkan SVR pada data pelatihan. Ini menunjukkan bahwa model Huber lebih baik dalam memprediksi biaya klaim berdasarkan data pelatihan.
Test MSE
Huber Regression juga menunjukkan kinerja yang lebih baik pada data pengujian. Ini menunjukkan bahwa model Huber tidak hanya belajar dengan baik dari data pelatihan tetapi juga dapat menggeneralisasi dengan baik ke data yang tidak terlihat.
Evaluasi Model
Eval Train dan Eval Test juga menunjukkan bahwa Huber Regression memiliki nilai yang lebih rendah dibandingkan SVR. Ini menunjukkan bahwa Huber Regression lebih konsisten dalam memprediksi biaya klaim di kedua set data.
3. Kesimpulan
Keunggulan Huber: Model Huber tampak lebih efektif dalam menangani data, terutama dengan mengurangi dampak outliers dan menghasilkan estimasi yang lebih akurat untuk biaya klaim.
Kelemahan SVR: SVR, meskipun dapat menangkap interaksi kompleks dalam data, tampaknya kurang efektif dalam konteks ini, mungkin karena kesulitan dalam menangani outliers atau ketidakcocokan antara model dan data.
Rekomendasi
Berdasarkan hasil ini, jika tujuan Anda adalah memprediksi biaya klaim secara akurat dan robust terhadap outliers, Huber Regression adalah pilihan yang lebih baik untuk dataset ini.
Namun, jika Anda ingin mengeksplorasi model yang lebih kompleks dan menangkap hubungan non-linear, Anda mungkin ingin meninjau kembali SVR dengan penyesuaian parameter yang lebih baik atau menggunakan teknik lain untuk menangani outliers.
"""

# get requirement.txt

!pip freeze > requirements.txt